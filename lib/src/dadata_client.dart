/// {@template dadata_client}
/// Dart package for Dadata.ru API integration. Provides address suggestions, data validation, and enrichment. Compatible with both Flutter and Dart.
/// {@endtemplate}
class DadataClient {
  /// {@macro dadata_client}
  const DadataClient();
}
